export enum Servers {
    // Quitar Alfombras (QA)
    server1 = 1,
    // Área de aspirado (AA)
    server2 = 2,
    // Lavado 1 (L)
    server3 = 3,
    // Lavado 2 (L)
    server32 = 32,
    // Secado (S)
    server4 = 4,
    // Pone alfombras (PA)
    server5 = 5,
}
