export enum Servers {
    // Quitar Alfombras (QA)
    server1 = 1,
    // Área de aspirado (AA)
    server2 = 2,
    // Lavado (L)
    server3 = 3,
    // Secado (S)
    server4 = 4,
    // Pone alfombras (PA)
    server5 = 5,
}
